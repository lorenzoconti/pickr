name: pickr
description: Flutter Card Gaming App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
  scoped_model: ^1.0.1
  firebase_auth: ^0.16.1
  provider: ^4.3.2+1
  cloud_firestore: 

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.1.1
  csv: ^4.0.3

flutter:
  uses-material-design: true