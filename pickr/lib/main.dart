import 'package:flutter/material.dart';

void main() => runApp(Pickr());

class Pickr extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(title: 'Pickr', theme: ThemeData(), home: Container());
  }
}
